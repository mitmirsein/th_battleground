# 📤 Note Share (JSP)

> **Role**: Obsidian 노트 공유 전문가
> **Objective**: 로컬 노트를 웹(Just Share Please)에 게시하여 외부 공유용 URL을 생성합니다.

## 🛠️ Capabilities

이 스킬은 `share.py` 스크립트를 사용하여 다음 작업을 수행합니다:

1.  **노트 공유**: 지정된 파일을 JSP 서버에 업로드하고 URL을 반환합니다.
2.  **업데이트**: 이미 공유된 노트의 내용을 최신으로 갱신합니다.
3.  **삭제**: 공유된 노트를 서버에서 삭제합니다.
4.  **목록 조회**: 현재 공유 중인 모든 노트의 목록을 보여줍니다.

## 🔑 Key Rules

1.  **Obsidian 연동**: 이 스킬은 Obsidian의 `just-share-please` 플러그인 설정(`data.json`)을 공유합니다. 따라서 Obsidian에서 공유한 것과 Antigravity에서 공유한 것이 동기화됩니다.
2.  **경로 처리**: 파일 경로는 반환된 `share.py`가 자동으로 `MS_Brain` 볼트 기준 절대 경로로 해석합니다. 상대 경로로 입력해도 됩니다.
3.  **보안**: 비밀번호(API Key)는 `data.json`에서 자동으로 로드됩니다.

## 🚀 How to Run

이 스킬은 `share.py` 스크립트를 실행하여 작동합니다.

### 1. 기본 공유 (또는 업데이트)
```bash
python .skills/note-share/scripts/share.py "경로/파일명.md"
```
*   기본적으로 새 파일이면 공유하고, 이미 공유된 파일이면 물어보거나 `-u` 옵션 안내가 뜹니다.

### 2. 강제 업데이트
```bash
python .skills/note-share/scripts/share.py --update "경로/파일명.md"
```

### 3. 공유 목록 확인
```bash
python .skills/note-share/scripts/share.py --list
```

### 4. 공유 삭제
```bash
python .skills/note-share/scripts/share.py --delete "경로/파일명.md"
```

## 📝 Trigger Examples

사용자가 다음과 같이 말하면 이 스킬을 실행하세요:

*   "이 노트 공유해줘"
*   "방금 쓴 글 JSP로 올려줘"
*   "공유 링크 만들어줘"
*   "공유된 파일 목록 보여줘"
